{
  "$schema": "./node_modules/wrangler/config-schema.json",
  // Static HTML/CSS/JS is served from the CDN without invoking this Worker, so
  // page views do not count against the free plan's 100k Worker requests/day.
  // Only `/api/*` runs Worker code (Groq ATS rewrite and resume import).
  "name": "resume-builder",
  "main": "workers/index.ts",
  "compatibility_date": "2026-09-16",
  "compatibility_flags": ["nodejs_compat"],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1,
  },
  "placement": {
    "mode": "smart",
  },
  "assets": {
    "directory": "./out",
    "html_handling": "auto-trailing-slash",
    "run_worker_first": ["/api/*"],
  },
  // Requires the zone to be active on this Cloudflare account first.
  // After that, `wrangler deploy` creates DNS + certificates for both hostnames.
  "routes": [
    { "pattern": "freeresumebuilder.co.in", "custom_domain": true },
    { "pattern": "www.freeresumebuilder.co.in", "custom_domain": true },
  ],
  "vars": {
    // Non-secret. Override in the dashboard if you pick a different Groq model.
    // GROQ_API_KEY is a secret: `npx wrangler secret put GROQ_API_KEY`
    "GROQ_MODEL": "openai/gpt-oss-120b",
  },
}
